let isDragging = false;

$('.wmg-pov-slider')
  .on('beforeChange', function () {
    isDragging = true;
    $('#wmg-pov-video-frame').css('pointer-events', 'none');
  })
  .on('afterChange', function () {
    isDragging = false;
    $('#wmg-pov-video-frame').css('pointer-events', 'auto');
  });
